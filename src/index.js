import { Board, Task, Project } from "./modules/taskboard";

import SaveManager from "./modules/saveManager";

const saveManager = new SaveManager();


const board = new Board("My board");

//testing
// const project = new Project("AGI", "desc");

// const task = new Task("Run", "desc", "low", { day: 26, month: 7, year: 2025 });


// project.addTask(task);
// board.addProject(project);

// board.save();
// board.delete();


// console.log(board.boardObj);
// board.populate();

// console.log(board.boardObj);











